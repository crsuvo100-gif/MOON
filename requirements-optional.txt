# ---------------------------------------------------------------------------
# MOON — OPTIONAL dependencies (best-effort; the installer installs these
# separately and does NOT fail if any are unavailable for your Python/platform).
# MOON degrades gracefully when these are absent.
# ---------------------------------------------------------------------------
# Embeddings / vector store (optional; pure-python fallback included)
# chromadb>=0.5
# sentence-transformers>=2.6

# Telegram channel (optional; MOON as a Telegram bot)
python-telegram-bot>=20.0

# Hugging Face (optional; HF InferenceClient: chat + text-to-image)
huggingface_hub>=0.22

# Tooling integrations (all optional)
requests>=2.31
beautifulsoup4>=4.12
playwright>=1.43
pillow>=10.3
pytesseract>=0.3
pypdf>=4.2
PyYAML>=6.0

# Voice engine (MOON's female voice + cloning; all optional)
# Kokoro-ONNX is the default premium female voice and is now in requirements.txt
# (offline, CPU, py3.13-compatible). espeak-ng+sox (system) is the fallback.
# vosk + pyaudio = offline speech-to-text (to HEAR and clone any user's voice).
# openai = cloud 'nova'/'shimmer' alluring female voices (needs OPENAI_API_KEY+quota).
# F5-TTS = zero-shot voice CLONING from a reference WAV, works on Python 3.13
# (torch 2.x wheels). This is the cloning engine used on modern Python; the
# cloning pipeline in app/voice_engine.py auto-activates when f5_tts is present.
# XTTS-v2 (Coqui `TTS`) is NOT listed: it requires Python <3.12 and fails to
# install on 3.13. On a py3.11 venv, `pip install TTS` also enables cloning.
f5-tts>=1.1
openai>=2.53
vosk>=0.3
pyaudio>=0.2

# Quality extras
pytest-cov>=5.0
ruff>=0.16
mypy>=1.9
textual>=0.80
