# ---------------------------------------------------------------------------
# MOON — CORE dependencies (required for the terminal HUD + agent runtime).
# These are resolved/installed first by install.sh and MUST succeed.
# ---------------------------------------------------------------------------
# Web framework / API
fastapi>=0.141
uvicorn[standard]>=0.52
pydantic>=2.13
pydantic-settings>=2.15

# Async runtime
anyio>=4.3
httpx>=0.27

# Model providers (OpenAI-compatible client -> points at your LOCAL model)
openai>=2.53

# Numerics / utilities
numpy>=1.26
python-dotenv>=1.0
rich>=13.7
tenacity>=8.2

# Testing / quality
pytest>=8.0
pytest-asyncio>=0.23

# NOTE: the old Textual TUI / flask orb were removed; the terminal UI is
# pure HTML/CSS/Canvas2D served by app.terminal_interface — no Python UI dep.
