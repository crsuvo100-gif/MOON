"""Backed up before removal — app/tui.py (Textual TUI) removed per user request.

MOON now has a single terminal type: the Hermes-style CLI REPL (moon run / moon
terminal / moon cli / bare `moon`). The Textual TUI (moon shell / moon tui) and
the dead app/cli_terminal.py fallback were removed — there is no web/HTTP UI.
"""
